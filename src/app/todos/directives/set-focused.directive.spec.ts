import { SetFocusedDirective } from './set-focused.directive';

describe('SetFocusedDirective', () => {
  it('should create an instance', () => {
    const directive = new SetFocusedDirective();
    expect(directive).toBeTruthy();
  });
});
